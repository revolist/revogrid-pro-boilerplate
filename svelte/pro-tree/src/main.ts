/**
 * RevoGrid Pro — Tree Plugin starter (Svelte)
 *
 * Required dependencies:
 *   @revolist/svelte-datagrid
 *   @revolist/revogrid-pro
 */

import App from './App.svelte';

const app = new App({ target: document.getElementById('app')! });

export default app;
