/**
 * RevoGrid Pivot starter (Vue 3)
 *
 * Required dependencies:
 *   @revolist/vue3-datagrid
 *   @revolist/pivot
 *   @revolist/revogrid-pro
 */

import { createApp } from 'vue';
import App from './App.vue';

createApp(App).mount('#app');
